# 交接文档 — 审题系统技术债清理收尾（2026-06-14）

> 给新窗口接替用，读完即可无缝继续。
> **项目：biology-exam-analyzer（高中试卷智能审题系统）**
> **位置：仅 jdcloud `~/biology-exam-analyzer`，本机/别处皆无。访问：`ssh jdcloud`**
> **分支：`upgrade/degoogle`｜HEAD：`97e58c7`｜基线：`4fa735c`**

---

## 一句话现状

2026-06-13 升级四目标（备份/摘谷歌/九学科/异步）全部收口 + 本轮技术债清理完成。**三容器健康，全程零功能回归，无进行中的未完成任务**。本会话已用 workflow 独立核实下方所有状态数字。

---

## 项目背景

- **栈**：FastAPI + React + PostgreSQL(pgvector)，docker compose 三容器：
  - `biology_backend`(8001→8000)、`biology_frontend`(3001→80)、`biology_postgres`(5432)
- **LLM**：DeepSeek V4 Pro(文本/推理) + Qwen3-VL(视觉)，**双国产，摘谷歌后无任何代理依赖**。统一入口 `llm_client.llm_call()`。
- **部署机制**：backend 卷挂载 `./backend:/app`（代码实时，改完 recreate 重启进程即可，**无需 rebuild**）；frontend 镜像烘焙 React build（改前端才需 rebuild）。
- **9 学科**：`backend/subject_config.py` 是九科核心素养维度**单一事实源**（语4/数6/英4/物4/化5/生4/政4/史5/地4）。
- **报告链**：`report_data.aggregate_report_data`(纯数据) → `report_insights.generate_insights`(LLM 文字：overall + teaching)。

---

## 本次会话做了什么（commit 链 `4fa735c..97e58c7`）

| commit | 内容 | 变化 |
|---|---|---|
| `23418a1` | **Goal A1**：report_insights 学科透传键路径对齐（加 `_data_subject()` 读 `exam_info.subject` 兜底 + 删死函数 `_build_comments_prompt`），消除非生物报告退「生物教研员」身份 | 1 file +7 −31 |
| `2cd2b0d` | **Goal A2**：per_subject_e2e leak 检测改结构化（competency_summary distribution keys ⊆ 本科维度，废弃 HTML 子串匹配），消除子能力细分误报 | 1 file +78 |
| `a3fd9f6` | **Goal B**：清谷歌死管道。B1 删 `exam_review_channel` 死参数链(router+service)；B2 删 llm_client `review_channel` ContextVar+set/reset+metadata死键；B3 `report_insights` grounding「去命名保功能」(`_build_grounding_facts`→`_build_evidence_facts`，prompt "Grounding Evidence Cards"→"数据事实卡片")；测试同步 | 8 files +40 −135 |
| `191d740` | **技术债 A/C/D**：A 删 `/api/analyze` 410下线端点不可达死体(~57行)+连带僵尸方法 `run_full_analysis`(~73行)；C `report.evidence_card.*`→`report.data_card.*` source 标签；D 核心4文件未用 import | 4 files +36 −176 |
| `97e58c7` | **autoflake 清预存未用 import**：全 backend 123→1（autoflake --remove-all-unused-imports --ignore-init-module-imports） | 39 files +31 −98 |

> 另：删了 untracked 一次性脚本（`backend/artifacts/` 4个含谷歌遗留 + `e2e_degoogle.py`/`e2e_subject.py`/`smoke_subject.py`）。这些是容器 root 创建的，用 `docker exec biology_backend rm` 删的。

---

## 当前状态（2026-06-14 workflow 核实）

- **git**：分支 `upgrade/degoogle`，HEAD `97e58c7`，基线 `4fa735c`，自基线 5 个 commit（见上表）
- **容器**：biology_backend healthy / biology_frontend up / biology_postgres healthy
- **摘谷歌验收**：`git grep -iE "grounding|review_channel|exam_review_channel|discovery|app_builder|evidence_client" -- backend/*.py` 排除 test/noqa 后 **非测试零命中**
- **未用 import**：pyflakes 全 backend = **1**（`test_integration_v2.py:34 import llm_client # noqa: F401`，**故意的副作用 import**防 sys.modules 污染，autoflake 正确保留，**勿删**）
- **pytest**：`tests/` + 4 个 metadata 测试 = **104 passed / 5 failed**。5 个 fail 是**预存**九学科测试债（非本会话引入，git stash 改前改后铁证一致）

---

## 🔴 红线（绝不能动，本次已逐项核实 INTACT）

| 红线 | 核实位置 | 值 |
|---|---|---|
| 九科素养维度 | `subject_config.py`(107行) | 语4/数6/英4/物4/化5/生4/政4/史5/地4 |
| 并发数 | `deps.py:15` | `MAX_WORKERS=21`（consumed deps.py:120） |
| DeepSeek 信号量 | `llm_config.py:46/64/80` | 15 / **30** / 15 |
| 报告 token 预算 | `report_insights.py:753/762/770/771` | `max_tokens=64000`（含降级 compact/ultra_compact） |
| 学科 prompt | `prompts/biology/` 等 | PromptLoader 渲染等价，生物保真 |
| 难度主轴 | `difficulty_pipeline.py` | content_difficulty + 三重 floor 护栏 + `DIFFICULTY_CALIBRATION_ENABLED`(默认关) |

> 注：`llm_policy.py`/`LLM_EXAM_REVIEW_FLASH/PRO_MODEL`/`LLM_SA_CREDENTIALS` 等谷歌锚点**摘谷歌时已整链删除**，新窗口无需理会（本机 memory `reference_biology_llm_architecture` 顶部已加作废警告）。
> **不碰前端**（除非用户明确要求，且前端禁 `up --build frontend` 连带重建 backend）。

---

## 待办（都无功能影响，纯可选，按价值排序）

1. **5 个预存 pytest fail**（低风险纯测试）：九学科 `build_competency_summary(analyzed, subject)`/`aggregate_statistics` 生产签名加了 subject 参数，但测试 mock `lambda questions: {}` 没跟上。涉及 `test_analysis_service_metadata.py`(x3) + `test_metadata_quality_visibility.py`(x2)。改 mock lambda 签名即可。
2. **22 处 f-string 占位符 lint 噪音**：`f"..."` 无 `{}` 占位符（功能等价于普通字符串）。位置见本机 memory 或 `python3 -m pyflakes backend/*.py | grep f-string`。去 `f` 前缀即可，零风险。
3. **时长压缩** 11.5→8-10min（⚠️**高风险尽力项**）：6min 已定案物理不可达，瓶颈在单题 reasoning 链路。动则易碰上方性能红线导致回归超时，需极谨慎。
4. **九科真卷内容质量验收**：需用户提供各科真实试卷素材，当前用 `test_9subjects` 内置真题验证。
5. **working tree 收尾**：`HANDOFF-2026-06-13-摘谷歌+九学科.md` 有未提交修改；3 个 untracked md 文档（`GOAL-2026-06-13-修复收尾-学科断点+清谷歌.md`、`GOAL-2026-06-13-升级目标.md`、`HANDOFF-审题模型-2026-06-11.md`）。用户尚未决定是否提交，**勿自行 commit**。

---

## 环境与工作流（接手必读）

- **访问**：`ssh jdcloud`（别名，纯 key 无 OTP）。审题项目**非受治理仓库**（不是 yuanshou/edu-cloud），可正常读写。
- **编辑模式**：本机无法直接对 jdcloud 文件用 Edit/Read。工作流 = `scp jdcloud:'biology-exam-analyzer/backend/X.py' 本机 → Edit → scp 回`。轻量改可远程 `sed`。
- **部署**：`docker compose up -d --force-recreate --no-deps backend`（⛔禁 `--build` 连带 libreoffice 慢构建；⛔不动前端镜像）。
- **删容器 root 创建的文件**：宿主机 ubuntu 无权 → `docker exec biology_backend rm /app/...`（卷挂载，宿主机同步消失）。
- **静态工具**：pyflakes 3.4.0 / autoflake 2.3.3 已 `pip install --user` 装在宿主机（`~/.local/bin` 不在 PATH，用 `python3 -m pyflakes` / `python3 -m autoflake` 调用）。
- **测试纪律**：别动不动跑全量（烧 token）。单元测试（`tests/` + `test_*metadata` 系列）mock 不烧 LLM 可跑；真卷 E2E（`per_subject_e2e.py`、`smoke_9subjects.py`）走真 LLM，谨慎。
- **commit**：结尾加 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。
- **远程 git**：配了 `receive.denyCurrentBranch updateInstead`（本地 push 自动更新工作树）。

---

## 关键指针

- **本机 memory**（`C:\Users\liang\.claude\projects\C--Users-liang\memory\`）：
  - `project_biology_perf_optimization.md`（主线，含本轮全部进展）
  - `reference_biology_llm_architecture.md`（LLM 架构 + 红线，⚠️顶部有摘谷歌后红线作废说明）
  - `project_biology_sync_timeout.md`（同步超时/报告捞回）、`reference_reasoning_model_budget.md`（推理模型预算陷阱）
- **GOAL 文档**：jdcloud `~/biology-exam-analyzer/GOAL-2026-06-13-修复收尾-学科断点+清谷歌.md`
- **验收脚本**：`backend/per_subject_e2e.py`（每科真卷 E2E）、`backend/smoke_9subjects.py`（九科 competency 贯通）
- **升级源交接**：jdcloud `~/biology-exam-analyzer/HANDOFF-2026-06-13-摘谷歌+九学科.md`
