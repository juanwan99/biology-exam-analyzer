# 审题系统升级交接文档 — 摘谷歌 + 九学科适配

**日期**：2026-06-13
**唯一位置**：京东云 `ssh jdcloud:~/biology-exam-analyzer`（不在本地/ECS/别处）
**工作分支**：`upgrade/degoogle`（HEAD `3e9e88f`，已推送到 jdcloud bare repo，配了 `receive.denyCurrentBranch updateInstead`）
**目标依据**：`GOAL-2026-06-13-升级目标.md`
**本会话改动**：47 文件，+2003 / −2512

---

## 一、四个目标的状态

| 目标 | 状态 | 验证 |
|---|---|---|
| 0 全量备份 | ✅ 完成 | `~/backups/bea-upgrade-20260613/`：项目 tar + git bundle + pg_dump + backend 镜像 + 恢复步骤.md |
| 1 摘谷歌 | ✅ 完成+部署+验证 | 真卷 E2E 报告产出、pytest 104 passed、零回归（63<71基线） |
| 2 九学科 | 🟡 后端完成+验证，前端待重建上线 | 化学 E2E 核心全过（5维正确）；冒烟 biology4/chemistry5 PASS |
| 3 异步化 | ✅ 核心已就位（先前重构+本会话补强） | analyze_auto 提交<3s 返回 task_id + 前端 localStorage 任务恢复 |
| 3尾 时长压缩 | ⏳ 未做（尽力项） | 6min 物理不可达定案；11.5→8-10min 待砍降级链 |

---

## 二、目标 1 摘谷歌（已完成，可放心）

**事实依据**：grounding 生产成功记录 0 条、preflight 因依赖缺失持续失败、报告全靠降级路径产出 → 零产出纯负担。

**删除**（commit `2232a0e`，−2363 行）：
- 整删 `llm_policy.py` / `services/review_channel.py` / `services/evidence_client_loader.py` / `test_review_channel.py`
- `report_insights.py` grounding 全链 386 行（_stabilize / _run_grounding_check / _build_grounding_sections 等）
- `question_analyzer.py` evidence 注入三参数与重排块
- `analysis_service.py` channel_usage / assert_channel_usage 门禁
- `llm_client.py` App Builder grounded generation 全链
- 远程：gitignored evidence 六件套 + `.credentials` + `.env` 谷歌段（残留0）
- **compose 脱 HTTPS_PROXY**（DeepSeek/Qwen 国产直连，代理依赖归零 = 最大稳定收益）

**保真**：reviewer 免扣费旁路、扣费 200、难度链、SEU 业务（evidence_units/missing_evidence_repair 等"名字带 evidence 但非谷歌"逻辑）全部保留。
**顺带修**：`62658e6` teaching 重试 retry_count 预存 latent bug（非本次引入）。

**GCP 云端项目本轮未删**（闲置无害），跑稳一个月后人工清理。

---

## 三、目标 2 九学科（后端完成验证，前端待重建上线）

### 架构（单一事实源）
`backend/subject_config.py` — 九科核心素养维度中枢，与 `prompts/{subject}/competency_prompt.txt` 输出键**严格一致**（已逐科校验）：

| 学科 | 维数 | 维度 |
|---|---|---|
| 语文 chinese | 4 | 语言建构与运用/思维发展与提升/审美鉴赏与创造/文化传承与理解 |
| 数学 math | 6 | 数学抽象/逻辑推理/数学建模/直观想象/数学运算/数据分析 |
| 英语 english | 4 | 语言能力/文化意识/思维品质/学习能力 |
| 物理 physics | 4 | 物理观念/科学思维/科学探究/科学态度与责任 |
| 化学 chemistry | 5 | 宏观辨识与微观探析/变化观念与平衡思想/证据推理与模型认知/科学探究与创新意识/科学态度与社会责任 |
| 生物 biology | 4 | 生命观念/科学思维/科学探究/社会责任 |
| 思想政治 politics | 4 | 政治认同/科学精神/法治意识/公共参与 |
| 历史 history | 5 | 唯物史观/时空观念/史料实证/历史解释/家国情怀 |
| 地理 geography | 4 | 人地协调观/综合思维/区域认知/地理实践力 |

关键 API：`get_competency_dims(subject)` / `get_subject_name(subject)` / `normalize_subject(subject)`（未知降级 biology）/ `competency_dims_from_distribution(dist, subject)`（数据驱动提维度）/ `competency_slug(name, i)`（生物维度保旧英文 slug，其余 dim{i}）。

### 已贯通（commit a3fee63 / 127ce9f / 37bfca5 / 6524ccd / 3e9e88f）
- **subject 全链穿线**：`analyze_auto` / `confirm_split` 加 `subject` Form 参数 → 拆题后注入 `question["subject"]` → service/analyzer/competency/difficulty 链路均从此取（无需逐函数改签名）
- **分析 prompt**：`question_analyzer.analyze_question` 改用 `PromptLoader(subject)`，优先 `analysis_prompt_v2`（仅生物有 SEU 细粒度，非生物回退 v1），prompt_id 动态学科前缀
- **素养**：`competency_analyzer` + `llm_schemas`（compute_summary_from_units/get_competency_weights 加 competency_dims 参数）按学科动态维度
- **报告层 N 维自适应**：`report_insights`（教研员/prompt_id 学科化）/ `report_product_charts`（雷达图维度+标签位置+矩阵图素养列全动态，角度法支持任意 N）/ `report_product_model`（缺口/分布/建议动态）/ `exam_diagnostics`（素养均衡动态）/ `report_product_html`（图描述去生物化）
- **DB**：`05_subject_column.sql` 4 表加 `subject` 列（默认 biology，迁移已在 jdcloud 应用）+ ORM models.py 对齐
- **前端**：`AnalyzerPage` 9 科选择器 + formData 传 subject；`ResultDisplay`/`ExamStatistics` 逐题素养/分布图遍历实际维度（不再写死4维）

### 验证
- **冒烟 PASS**：biology→4维+biology prompt；chemistry→5维+chemistry prompt（`backend/smoke_subject.py`）
- **化学报告 E2E**（生物卷以 subject=chemistry 验报告管线，`backend/e2e_subject.py`）：分析 21/21、competency_summary 与报告 HTML 均化学5维、PDF+HTML 产出。
- **生物零回归**：subject 缺省 biology、`prompts/biology/` 经 PromptLoader 渲染等价（feature_extractor 早在生产用此路径佐证）、competency_slug 对生物返回既有英文 slug。

### 待办（剩余）
1. **🔴 前端重建上线**：subject 选择器 + 动态素养 UI 改了源码，**需重建 frontend 镜像才生效**（本会话已后台触发，需确认）：
   ```
   ssh jdcloud 'cd ~/biology-exam-analyzer && docker compose build frontend && docker compose up -d --no-deps --force-recreate frontend'
   ```
   ⚠️ 不要用 `docker compose up --build frontend`（会因 depends_on 连带重建 backend，触发 libreoffice 慢构建）。务必 `build frontend` + `--no-deps`。
2. **九科真卷 E2E**：现仅用生物卷验证了管线。每科应跑 ≥1 份真卷（拆题/分析/报告/素养维度符合课标）。各科真卷需准备（用户提供或公开真题构造 docx）。
3. **knowledge_mapper 教材章节树仅生物**（降级共识：非生物知识点不分章节，报告章节分析降级，可接受）。九科教材树数据化是后续增强。
4. **题库相似题**：exercise_bank 701 题全生物；非生物相似题留空（降级共识）。

---

## 四、目标 3 异步化（核心已就位）

查证：`analyze_auto` 早已异步化（提交 <3s 返回 task_id + 后台 asyncio 任务 + `/api/analysis/status/{task_id}` 轮询）。memory 里"客户端 10 分钟 499 假失败"已被先前重构根治（提交即返回，长任务后台跑）。
本会话补强：前端 `bio_task_id` localStorage 任务恢复（浏览器关掉重开仍可跟踪后台任务）。
残留：砍 7 层降级链（简化失败路径，既提稳定又省时间，未做）。

---

## 五、红线（绝不能动）
- reviewer 免扣费旁路（前端 IP 检测 + 后端 `_resolve_user_and_review` username=="reviewer" 跳过扣费）
- 扣费金额 200、难度链核心逻辑、`DIFFICULTY_CALIBRATION_ENABLED` 默认关
- SEU 业务语义（evidence_units/diagnostic_units/stimulus_units/missing_evidence_repair）—— 名字带 evidence 但与谷歌无关，删了断功能

---

## 六、已知问题 / 风险

1. **【预存，非本次】大题 media 严格门不一致**：`run_auto_analysis`/`generate_report` 路径的 `assert_pipeline_ready` 对"含图大题 media_not_passed"硬阻断，但生产 `analyze_auto` 路径（`validate_report_metadata` + `_generate_route_report_artifacts`）只记非阻断警告 → **生产用户能出报告**。该严格门在安全加固 `30b719a` 引入，a3c959e 即存在，非 degoogle/九学科 引入。split-merge 大题 media 穿线在某处断（大题分析可能没看到图）。**待用户裁定是否修**。
2. **测试套件预存隔离污染**：全量 pytest 63 failed（基线 71），单独跑各文件全过。是测试间共享单例/env 顺序问题，非功能缺陷。九学科改动后未重跑全量（degoogle 模块 104 passed）。
3. **CompetencyResult schema 仍声明生物4维命名字段**（extra="allow" 兼容其他科）：非生物素养作 extra 字段被接受，聚合/渲染走动态维度正确；唯 schema introspection 处可能显示生物字段名（已修方法论审计区，其余暂未见）。

---

## 七、关键文件索引

| 文件 | 作用 |
|---|---|
| `backend/subject_config.py` | 九科素养中枢（单一事实源） |
| `backend/smoke_subject.py` | 学科贯通冒烟（biology/chemistry 维度+prompt 路由） |
| `backend/e2e_subject.py` | 九学科报告管线 E2E（参数化 subject） |
| `backend/e2e_degoogle.py` | degoogle 生产路径 E2E（真报告产出） |
| `database/init/05_subject_column.sql` | subject 列迁移（幂等） |
| `prompts/{subject}/` | 9 科 ×6 prompt 模板（competency_prompt 维度=subject_config） |
| `~/backups/bea-upgrade-20260613/恢复步骤.md` | 升级失败回滚步骤 |

## 八、部署/回滚速查
```bash
# 部署 backend（卷挂载代码实时，只需 recreate，勿 --build 除非依赖变更）
ssh jdcloud 'cd ~/biology-exam-analyzer && docker compose up -d --force-recreate backend'
# 部署 frontend（必须重建镜像 + --no-deps）
ssh jdcloud 'cd ~/biology-exam-analyzer && docker compose build frontend && docker compose up -d --no-deps --force-recreate frontend'
# 健康检查
ssh jdcloud 'curl -s http://127.0.0.1:8001/health'
# 回滚：见 ~/backups/bea-upgrade-20260613/恢复步骤.md
```
