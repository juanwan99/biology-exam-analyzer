# Subject: math
# Purpose: feature_difficulty —— 难度核心组（雷达图依赖的难度6维 + bloom，最高优先级、必须可靠收敛）
# Source: 方案B 拆分自 backend/feature_extractor.py::build_feature_prompt()
# Variables: {question_block}, {qtype_hint}
你是一名资深高中数学命题审查专家。只做难度预测分析，不评质量、不写教学点评。

题目：
{question_block}{qtype_hint}

只输出严格 JSON（不要 markdown、不要解释、不要省略号），且只包含以下键：
{
  "working_memory": 1-5（解题关键步骤中需同时在脑中保持的信息元素数量。1=直接匹配，2=单一比较，3=多条件筛选(3-4个条件)，4=多要素联立(4-5个信息交叉)，5=复杂系统推理(5+要素，如含参分类讨论+多步联立化简联合)），
  "working_memory_reason": "列出关键步骤需同时处理的具体信息元素(≤40字)",
  "reasoning_steps": 正整数（解出本题所需的最少认知操作数，不含读题。选择题须计入判断/排除每个有迷惑性选项所需的操作，不是只算找到正确答案的最短路径），
  "steps_detail": "简述推理链(≤50字)",
  "chain_coupling": 1-3（解题推理过程的依赖深度，评估"推理链"本身、而非"选项之间是否独立"。1=独立：靠孤立公式回忆或单步判断，各步互不依赖；2=部分依赖：部分判断需基于前一步结论或共享同一图象/同一组条件；3=全链依赖：前一步错后续全错，如求导→定单调区间→求极值→讨论参数连环推导、解析几何联立→韦达定理→弦长/定点链式计算、数列归纳证明，或单个选项内部需"数形结合→推断→定范围"的链式推导。注意：选项之间互不影响≠chain_coupling=1——若逐项判断都基于同一函数图象/同一组条件做多步推导，或存在链式推理的选项，应判2-3），
  "coupling_reason": "说明为什么是该耦合度(≤30字)",
  "trap_density": 1-3（看似正确但实际错误的推理路径或选项数量。1=低(0-1个有效干扰)，2=中(2-3个有迷惑性)，3=高(4+个看似合理的错误路径或经典易混淆陷阱)），
  "trap_reason": "指出主要陷阱是什么(≤30字)",
  "novelty": 1-3（1教材原文/常见原题 2变式/改编 3全新情境/陌生素材），
  "novelty_reason": "一句话(≤20字)",
  "knowledge_breadth": 1-3（1单知识点 2跨考点 3跨模块），
  "breadth_reason": "一句话(≤20字)",
  "bloom": 1-6（该题所需的最高认知层级，仅用于教学报告标签，不影响难度评分），
  "bloom_reason": "描述最高层级对应的具体认知操作(≤30字)"
}

**working_memory 判例：** 1直接回忆单一公式/定义；2两个量比较（如比较两函数值或两数列项大小）；3=3-4个条件同时筛选；4=4-5个信息交叉（含参讨论、解析几何联立化简、空间向量多坐标、分布列多取值通常≥4）；5=5+要素系统推理（导数含参全分类讨论+放缩证明）
**chain_coupling 判例：** 孤立公式回忆型选择题=1；各选项需共享同一函数图象/同一组条件做多步推导、或含"数形结合→推断→定范围"链式选项=2~3；求导+单调+极值+讨论连环、解析几何联立+韦达+定值全链=3。（"选项独立"指选项之间，不代表解题推理低耦合）
**trap_density 判例：** 只1个有效干扰项=1；2-3个选项有迷惑性=2；所有选项"看起来对"或多个经典混淆点（如忽略定义域、含参讨论漏分类、用E(X)代替E(Y)、忽略向量共线/垂直条件、分类讨论端点取舍）=3
**bloom：** bloom=5对应"证明/论证/判断命题是否成立/据期望做决策"，bloom=6对应"构造函数证明不等式/设计统计方案"
